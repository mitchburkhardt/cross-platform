<%
	var bottom = '';
	var script;
	if(locals.fullPage){
		bottom = include('../../includes/closeTemplate', locals);
	}
	else{
		script = include('./page.js');
		bottom = `<script>
			app.pageScripts['${locals.filename}'] = function(){
				${script}
			};
			app.pageScripts['${locals.filename}']();
		</script>`;
	}

%>

<%- bottom %>
